import 'package:flutter/widgets.dart';

class AddNewTaskPageBody extends StatelessWidget {
  const AddNewTaskPageBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
